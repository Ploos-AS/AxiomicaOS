# Copy values into the service environment of the dedicated runtime runner.
# Never commit the real ROM path if it reveals private host layout.
AXIOMICA_KICKSTART_ROM=/srv/axiomica/roms/amiga-a500.rom
AXIOMICA_RUNTIME_TIMEOUT=30
